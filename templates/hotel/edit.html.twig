{% extends 'base.html.twig' %}

{% block title %}Edit Hotel{% endblock %}

{% block body %}
    <div class="panel">
        <div class="section-heading">
            <h1>Edit Hotel</h1>
            <div class="actions">
                <a class="button secondary" href="{{ path('app_hotel_index') }}">Back to list</a>
            </div>
        </div>

        {{ include('hotel/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="actions">
            {{ include('hotel/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
